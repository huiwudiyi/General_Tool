name: tool-description-optimizer
description: 基于测试集的召回评估、语义/功能一致性检查、生成质量检查以及 Pass@K 前后比较来优化技能/工具卡描述。

# Tool Description Recall Optimizer Skill

工具描述准召评测与自动优化 Skill，用于基于工具 `title` 与 `description` 构建召回底库，使用验证集计算准召指标，收集未召回 case，并迭代优化工具描述。

## 能力范围

- 基于 `工具名称 + 工具描述` 构建工具召回文本。
- 支持注入真实 embedding 模型，也提供离线可运行的 `SimpleHashEmbeddingEncoder`。
- 计算 `pass@k`、`recall@k`、`precision@k`、`mrr`。
- 输出未召回 query、标准工具、召回工具和误召工具。
- 按 `tool_id` 聚合失败 case。
- 支持注入 LLM 描述优化器，也提供安全的启发式兜底优化器。
- 使用 exact / normalize / semantic similarity 三层描述变更校验。
- 多轮迭代，保留历史最优描述版本并在指标退化时回滚。

## 输入格式

### tools.json

```json
[
  {
    "id": "idx1",
    "title": "天气查询",
    "description": "用于查询指定城市的天气情况"
  }
]
```

### validation.json

```json
[
  {
    "query": "北京明天会不会下雨",
    "positive_tool_ids": ["idx1"]
  }
]
```

## 命令行使用

```bash
python LLM/tool_description_recall_optimizer/optimizer.py \
  --tools tools.json \
  --validation validation.json \
  --output reports/tool_recall_report.json \
  --topk 1,3,5,10 \
  --max-iter 5
```

## Python API

```python
from LLM.tool_description_recall_optimizer import Tool, ValidationItem, optimize_tool_descriptions

report = optimize_tool_descriptions(
    tools=[Tool(id="idx1", title="天气查询", description="用于查询指定城市的天气情况")],
    validation_set=[ValidationItem(query="北京明天会不会下雨", positive_tool_ids=["idx1"])],
    topk_list=[1, 3, 5],
    max_iter=3,
)
```

## 接入真实模型

生产环境可注入自定义对象：

- `encoder.encode(texts: Sequence[str], batch_size: int) -> vectors`
- `description_optimizer.optimize(group: dict, optimize_batch_size: int) -> dict`

`description_optimizer.optimize` 建议返回：

```json
{
  "tool_id": "idx1",
  "optimized_description": "优化后的工具描述",
  "change_reason": "为什么这样优化",
  "added_semantics": ["新增语义点"],
  "risk": "none"
}
```

## 输出报告

核心输出包括：

- `final_tools`：最优轮次工具描述集合。
- `best_metrics`：最优指标。
- `best_iter`：最优迭代轮次。
- `iteration_reports`：每轮指标、missed case 数、变更工具数和优化记录。
- `version_history`：工具描述版本历史。